name: skill-router
version: 3.1.0
description: "统一技能路由 v3.1 — 双后端（MiniLM/SkillRouter）+ 完整 body 嵌入 + core/pool 分层"
kind: backend
author: hermes-agent
hermes_compat: ">=2.0.0"
provides_tools:
  - skill_search
  - skill_feedback
provides_hooks:
  - pre_llm_call
